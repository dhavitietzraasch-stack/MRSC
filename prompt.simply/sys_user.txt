━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MRSC — sys.user  v4.0
User session configuration file.
Fill in the fields below before starting.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

language:       // ex: English | Português (BR) | Español
user:           // your name or alias
IA:             // Claude | ChatGPT | Gemini | other

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION PREFERENCES (optional)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

response_style:     // short | medium | detailed
tone:               // neutral | formal | casual | sarcastic
strict_mode:        // on | off  (default: off)
kernel_optional:    // on | off  (default: off — loads security layer)
auto_confirm:       // on | off  (default: off — require ? confirmation)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// This file is loaded once at session start.
// Values here act as global defaults.
// They can be overridden at any time with:
//   set($field) = "new_value"
// or by declaring a rule that changes them.

// Example — change tone mid-session:
//   set($tone) = "formal" #global

// Example — enable strict mode:
//   /strict on
